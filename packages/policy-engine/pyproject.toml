[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "policy-engine"
version = "0.1.0"
description = "Server-side policy evaluation engine, gate sequence, and approval governance for autonomous self-healing."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
    "sqlalchemy>=2.0.0",
]
